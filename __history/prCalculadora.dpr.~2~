program prCalculadora;

uses
  System.StartUpCopy,
  FMX.Forms,
  Unit1 in 'Unit1.pas' {frCalculadora};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TfrCalculadora, frCalculadora);
  Application.Run;
end.
